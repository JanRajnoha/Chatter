<Window x:Class="Chatter.Views.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Chatter.Views"
        mc:Ignorable="d"
        ResizeMode="NoResize" WindowStyle="None" Background="White"
        Title="SplashScreen" Height="400" Width="400">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Image Grid.Row="1"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               Width="200" 
               Source="/Chatter;component/Images/chatter_logo_use.png"/>

        <Label Grid.Row="3"
               FontStyle="Normal"
               VerticalContentAlignment="Center"
               FontSize="40"
               FontWeight="Medium"
               HorizontalAlignment="Center"
               Content="CHATTER"/>

    </Grid>
</Window>
